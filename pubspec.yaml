name: jawara_mobile_v2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1          # State management
  http: ^1.1.0              # HTTP client
  shared_preferences: ^2.2.2 # Local storage
  go_router: ^12.1.3
  google_fonts: ^4.0.4
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  file_picker: ^10.3.7
  image_picker: ^1.2.1      # Image picker for payment proof
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.5
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
